/** Medios compartidos del hero (inicio ↔ asesoría). */

/** Video local de desarrollo / fallback del hero y del feed.
 * Cuando subes desde el panel, manda `hero_video_url` / `video_url` (S3).
 */
export const HOME_HERO_VIDEO = "/videos/asesoria-hero.mp4";

/**
 * Fotografía de portada del hero de Asesoría (antes en Inicio).
 * Misma URL por defecto que usaba HeroSection / home-content-mappers.
 */
export const ASESORIA_HERO_IMAGE =
  "https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?q=80&w=2070&auto=format&fit=crop";
